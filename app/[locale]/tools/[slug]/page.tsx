import {notFound} from 'next/navigation';
import ToolScreen from '@/components/tool-screen';
import {getTool,categories,type Locale} from '@/lib/tools';
import {origin,alternate} from '@/lib/seo';
export async function generateMetadata({params}:{params:Promise<{locale:Locale;slug:string}>}){const {locale,slug}=await params;const tool=getTool(slug);if(!tool)return {};return {title:tool.title[locale]+' · Nexo',description:tool.description[locale],alternates:alternate(locale,'/tools/'+slug)};}
export default async function Page({params}:{params:Promise<{locale:Locale;slug:string}>}){const {locale,slug}=await params;const tool=getTool(slug);if(!tool||(locale!=='es'&&locale!=='en'))notFound();const cat=categories.find(c=>c.id===tool.category)!;const schema=[{'@context':'https://schema.org','@type':'WebApplication',name:tool.title[locale],description:tool.description[locale],applicationCategory:'UtilitiesApplication',operatingSystem:'Any',inLanguage:locale,url:`${origin}/${locale}/tools/${slug}`},{'@context':'https://schema.org','@type':'BreadcrumbList',itemListElement:[{'@type':'ListItem',position:1,name:locale==='es'?'Herramientas':'Tools',item:`${origin}/${locale}`},{'@type':'ListItem',position:2,name:cat.title[locale],item:`${origin}/${locale}/category/${cat.id}`},{'@type':'ListItem',position:3,name:tool.title[locale],item:`${origin}/${locale}/tools/${slug}`}]}];return <><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema).replace(/</g,'\\u003c')}}/><ToolScreen id={slug} locale={locale}/></>;}
