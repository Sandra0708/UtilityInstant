import {tools,categories} from '@/lib/tools';
import {origin} from '@/lib/seo';
export function GET(){const paths=['',...tools.map(t=>'/tools/'+t.id),...categories.map(c=>'/category/'+c.id)];const rows=['es','en'].flatMap(locale=>paths.map(path=>`<url><loc>${origin}/${locale}${path}</loc><xhtml:link rel="alternate" hreflang="es" href="${origin}/es${path}"/><xhtml:link rel="alternate" hreflang="en" href="${origin}/en${path}"/></url>`));return new Response(`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9" xmlns:xhtml="http://www.w3.org/1999/xhtml">${rows.join('')}</urlset>`,{headers:{'Content-Type':'application/xml'}});}
