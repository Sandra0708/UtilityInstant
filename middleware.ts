import {NextResponse,type NextRequest} from 'next/server';
import {domainRedirect} from './lib/domains';
const spanishCountries=new Set(['ES','MX','AR','CO','CL','PE','VE','UY','PY','BO','EC','CR','PA','DO','GT','HN','SV','NI','CU','PR','GQ']);
function preferredLocale(request:NextRequest){const country=(request as NextRequest & {cf?:{country?:string}}).cf?.country?.toUpperCase();if(country&&spanishCountries.has(country))return 'es';const language=request.headers.get('accept-language')?.toLowerCase()||'';return /(^|,)\s*es(?:-|,|;|$)/.test(language)?'es':'en';}
export function middleware(request:NextRequest){const redirect=domainRedirect(request.url);if(redirect)return NextResponse.redirect(redirect,308);if(request.nextUrl.pathname==='/'){const url=request.nextUrl.clone();url.pathname='/'+preferredLocale(request);const response=NextResponse.redirect(url,302);response.headers.set('Vary','Accept-Language, CF-IPCountry');response.headers.set('Cache-Control','private, no-store');return response;}const headers=new Headers(request.headers);headers.set('x-nexo-locale',request.nextUrl.pathname.split('/')[1]==='en'?'en':'es');return NextResponse.next({request:{headers}});}
export const config={matcher:['/((?!_next|__debug).*)']};
